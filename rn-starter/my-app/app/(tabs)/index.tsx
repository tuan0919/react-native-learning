import { Image, StyleSheet, Platform } from 'react-native';
import ComponentsScreen from '@/components/screen/ComponentScreen';

export default function HomeScreen() {
  return (
    <ComponentsScreen></ComponentsScreen>
  );
}